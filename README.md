# kacc_crypto

Crypto acceleration source work for BoostKit.

The `dev` branch keeps the SVE2 AES-XTS source files and helper scripts for
iterative development.  Use `scripts/apply_and_test_xts.sh` to install the
source overlay into an OpenSSL tree and run the validation/performance tests.
